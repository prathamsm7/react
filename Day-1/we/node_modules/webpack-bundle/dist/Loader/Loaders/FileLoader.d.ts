import { Loader } from "../Loader";
export declare class FileLoader extends Loader {
    protected getDefaults(): {
        test: RegExp;
        loader: string;
        options: {
            name: string;
            outputPath: string;
        };
    };
}
