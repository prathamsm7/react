import { Loader } from "../Loader";
export declare class LessLoader extends Loader {
    protected getDefaults(): {
        test: RegExp;
        use: {
            loader: string;
        }[];
    };
}
