import { Loader } from "../Loader";
export declare class SassLoader extends Loader {
    protected getDefaults(): {
        test: RegExp;
        use: {
            loader: string;
        }[];
    };
}
