import { Compiler, WebpackPluginInstance } from "webpack";
import { ObjectOption } from "../ObjectOption";
export declare type Plugin = ((this: Compiler, compiler: Compiler) => void) | WebpackPluginInstance;
export declare class Plugins extends ObjectOption<Plugin[]> {
    protected readonly value: Plugin[];
    constructor(value?: Plugin[]);
    get key(): string;
    add(plugin: Plugin): this;
    get(): Plugin[];
    replace(plugin: Plugin): this;
}
